<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Reviews</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: url('https://images.unsplash.com/photo-1521747116042-5a810fda9664') no-repeat center center fixed;
      background-size: cover;
      color: #333;
    }

    .navbar {
      background-color: rgba(0, 0, 0, 0.8);
    }

    .navbar-brand {
      color: #fff !important;
      font-weight: 600;
      font-size: 24px;
    }

    .navbar-brand span {
      color: #fca311;
    }

    .nav-link {
      color: #fff !important;
      font-weight: 600;
    }

    .nav-link:hover {
      color: #fca311 !important;
    }

    .container {
      background: rgba(255, 255, 255, 0.85);
      padding: 30px;
      margin-top: 50px;
      border-radius: 10px;
      box-shadow: 0px 10px 25px rgba(0, 0, 0, 0.2);
    }

    h1 {
      font-family: 'Playfair Display', serif;
      text-align: center;
      font-weight: 600;
      color: #fca311;
      margin-bottom: 20px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background-color: #fff;
      margin-top: 20px;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }

    thead th {
      background-color: #343a40;
      color: #fff;
      padding: 12px;
    }

    tbody tr:hover {
      background-color: #f1f1f1;
    }

    td, th {
      padding: 12px;
      text-align: left;
    }

    .rating {
      font-weight: bold;
      color: #28a745;
    }

    .no-reviews {
      text-align: center;
      font-style: italic;
      color: #6c757d;
    }

    hr {
      border-color: #fca311;
    }

    h5 {
      text-align: center;
      font-family: 'Playfair Display', serif;
      color: #fca311;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <span>Database</span> Mobiles
      </a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav">
          <li class="nav-item"><a class="nav-link" href="/Bootstrap">Visit Main Site</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container">
    <h1>All Reviews</h1>
    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th>Products</th>
          <th>Rating</th>
          <th>Comment</th>
          <th>Date</th>
        </tr>
      </thead>
      <tbody>
        <% if (reviews.length === 0) { %>
          <tr>
            <td colspan="6" class="no-reviews">No reviews found.</td>
          </tr>
        <% } else { %>
          <% reviews.forEach(review => { %>
            <% review.orders.forEach(order => { %>
              <tr>
                <td><%= order.userId.name %></td>
                <td><%= order.userId.email %></td>
                <td>
                  <ul>
                    <% order.products.forEach(product => { %>
                      <li><%= product.title %></li>
                    <% }); %>
                  </ul>
                </td>
                <td class="rating"><%= review.rating %></td>
                <td><%= review.comment %></td>
                <td><%= new Date(review.date).toLocaleDateString() %></td>
              </tr>
            <% }) %>
          <% }) %>
        <% } %>
      </tbody>
    </table>
  </div>

  <hr>
  <h5>Database Mobiles &copy; 2024</h5>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
