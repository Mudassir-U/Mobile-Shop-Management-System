<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Database Mobiles</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f9fa;
        }

        /* Header */
        .navbar {
            background-color: #343a40;
        }

        .navbar-brand {
            margin-left: 30px;
        }

        .navbar-brand,
        .nav-link {
            color: white !important;
            font-weight: 600;
        }

        .nav-link:hover,
        .navbar-brand:hover {
            color: #adb5bd !important;
            transition: all 0.3s ease-in-out;
        }

        .navbar-brand img {
            height: 40px;
            margin-right: 10px;
        }

        /* Section Title */
        h4 {
            font-family: 'Playfair Display', serif;
            color: #333;
            text-align: center;
            margin-top: 50px;
            font-weight: 600;
        }

        /* Product Card Styling */
        .card {
            border: none;
            border-radius: 5px;
            box-shadow: 0px 5px 5px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0px 15px 25px rgba(0, 0, 0, 0.1);
        }

        .card-body {
            padding: 20px;
        }

        .card-title-heading {
            font-size: 1.1rem;
            font-weight: 600;
            color: grey;
            margin-bottom: 5px;
            text-transform: uppercase;
        }

        .card-description {
            color: #333;
            font-size: 1rem;
            margin-bottom: 10px;
        }

        .card-price {
            font-size: 1.3rem;
            font-weight: 700;
            color: #28a745;
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
            padding: 12px 25px;
            font-weight: 600;
            text-transform: uppercase;
            border-radius: 30px;
            transition: all 0.3s ease-in-out;
            margin-top: auto;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.2);
        }

        /* Footer */
        footer {
            background-color: #343a40;
            color: #fff;
            padding: 30px;
            text-align: center;
            margin-top: 50px;
        }

        footer a {
            color: #adb5bd;
            text-decoration: none;
            font-weight: 600;
        }

        footer a:hover {
            color: white;
            text-decoration: underline;
        }

        /* Adjusted Header Spacing */
        header {
            background-color: #343a40;
            padding: 15px 0;
        }

        header img {
            width: 300px;
            height: auto;
        }

        header .nav-buttons {
            margin-left: 30px;
            display: inline-block;
        }

        header .nav-buttons a {
            color: white;
            font-weight: 600;
            margin: 0 15px;
            text-decoration: none;
            font-size: 16px;
        }

        header .nav-buttons a:hover {
            color: #adb5bd;
            transition: color 0.3s ease-in-out;
        }
    </style>
</head>

<body>

    <!-- Header with Logo and Text Buttons -->
    <header class="text-white p-3">
        <div class="container d-flex justify-content-start align-items-center">
            <img src="206805728.png" alt="Database Mobiles Logo">
            <div class="nav-buttons">
                <a href="/cart">Cart</a>
                <a href="/customer-order">My Order</a>
                <a href="/reviews">My Reviews</a>
                <a href="/Bootstrap">Visit Main Site</a>
            </div>
        </div>
    </header>

    <!-- Featured Products Section -->
    <div class="container">
        <h4>My Featured Products</h4>
        <div class="row">
            <% for (let index = 0; index < products.length; index++) { %>
                <div class="col-lg-4 col-md-6 col-sm-12 mb-4">
                    <div class="card">
                        <img src="/<%= products[index].picture %>" class="card-img-top" alt="<%= products[index].title %>">
                        <div class="card-body">
                            <h6 class="card-title-heading">TITLE</h6>
                            <p class="card-description"><%= products[index].title %></p>
                            <h6 class="card-title-heading">Description</h6>
                            <p class="card-description"><%= products[index].description %></p>
                            <h6 class="card-title-heading">Availability</h6>
                            <% if (products[index].stock >= 1) { %>
                                <button style="border-radius: 20px; font-size: 20px; font-weight: bold; margin-bottom: 10px; background-color: green;" class="btn btn-success" disabled>
                                    Available
                                </button>
                                <div>
                                    <a href="/add-to-cart/<%= products[index]._id %>" style="display: block; text-align: center; margin-top: 10px;" class="btn btn-primary">
                                        Add To Cart
                                    </a>
                                </div>
                            <% } else { %>
                                <button style="border-radius: 20px; font-size: 20px; font-weight: bold; margin-bottom: 10px;" class="btn btn-danger" disabled>
                                    Not Available
                                </button>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 Database Mobiles. All rights reserved</p>
    </footer>

    <!-- Bootstrap Bundle JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>
