<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Database Mobiles Admin Panel</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* General Body Styling */
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background: url('https://images.unsplash.com/photo-1521747116042-5a810fda9664') no-repeat center center fixed;
            background-size: cover;
            color: #333;
        }

        /* Navbar Styling */
        .navbar {
            background-color: rgba(0, 0, 0, 0.8);
        }

        .navbar-brand {
            display: flex;
            align-items: center;
            color: #fff !important;
            font-weight: 600;
            font-size: 24px; /* Adjust the font size of the title */
        }

        .navbar-brand span {
            color: #fca311; /* Adjust the color of the title */
        }

        .nav-link {
            color: #fff !important;
            font-weight: 600;
        }

        .nav-link:hover {
            color: #fca311 !important;
            transition: all 0.3s ease-in-out;
        }

        /* Transparent Container */
        .container {
            background: rgba(255, 255, 255, 0.85); /* Semi-transparent white */
            padding: 30px;
            margin-top: 50px;
            border-radius: 10px;
            box-shadow: 0px 10px 25px rgba(0, 0, 0, 0.2);
        }

        /* Button Styling */
        .btn-create {
            background-color: #28a745;
            color: #fff;
            border: none;
            font-weight: 600;
            border-radius: 30px;
            padding: 10px 25px;
            text-transform: uppercase;
            transition: all 0.3s ease-in-out;
        }

        .btn-create:hover {
            background-color: #218838;
            transform: translateY(-3px);
            box-shadow: 0px 5px 15px rgba(40, 167, 69, 0.4);
        }

        .btn-edit {
            background-color: #17a2b8;
            color: #fff;
            border: none;
            font-weight: 600;
            border-radius: 30px;
            padding: 10px 20px;
            text-transform: uppercase;
            transition: all 0.3s ease-in-out;
        }

        .btn-edit:hover {
            background-color: #138496;
            transform: translateY(-3px);
            box-shadow: 0px 5px 15px rgba(23, 162, 184, 0.4);
        }

        .btn-custom {
            background-color: #ffc107;
            color: #333;
            border: none;
            border-radius: 30px;
            padding: 10px 25px;
            font-weight: 600;
            transition: all 0.3s ease-in-out;
        }

        .btn-custom:hover {
            background-color: #e0a800;
            color: #fff;
            transform: translateY(-3px);
            box-shadow: 0px 5px 15px rgba(255, 193, 7, 0.4);
        }

        /* Footer Styling */
        h5 {
            text-align: center;
            font-family: 'Playfair Display', serif;
            font-weight: 600;
            color: #fca311;
            margin-top: 20px;
        }

        hr {
            border-color: #fca311;
            opacity: 1;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/admin/product/:page?">
                <span>Database</span> Mobiles
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/order">ORDERS</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/customer-reviews">CUSTOMER-REVIEWS</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Bootstrap">VISIT MAIN SITE</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container">
        <h2 class="text-center mb-4">Welcome to Database Mobiles Admin Panel</h2>
        
        <%- body %>
    </div>

    <!-- Footer -->
    <hr>
    <h5>Database Mobiles Admin Panel &copy; 2024</h5>
    <%- defineContent("footer-area") %>

    <!-- Bootstrap Bundle JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
